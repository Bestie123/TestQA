{
  "$schema": "https://opencode.ai/config.json",
  "command": {
    "push": {
      "description": "Отправить все закоммиченные изменения на GitHub (git push)",
      "template": "Выполни git push в терминале для текущего репозитория. Если возникнет ошибка аутентификации — используй GitHub CLI: gh auth login"
    }
  }
}
